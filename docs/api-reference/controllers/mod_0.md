# mod_0

[← Back to API Reference catalog](../index.md)

## Overview

- **Module identity:** `mod_0`
- **Layer:** `controllers`
- **Source file:** `src/controllers/file_0.js`
- **Functions:** 1,200 (`mod_0_0` … `mod_0_1199`)

`mod_0` is one of three module identities in the `controllers` layer — alongside `mod_11` and `mod_22` — and, like every identity in the codebase, it contains only the uniform arithmetic functions, with no business logic.

> **Synthetic code.** Despite the `controllers` layer name, `mod_0` performs no request handling; every one of its functions is the same synthetic arithmetic routine.

## Uniform Contract

Every function in `mod_0` shares the signature `mod_0_M(x) → number`: it accumulates `x*1 + x*2 + x*3` (= `6*x`) and adds `10` when the total is even, so for integer `x` the result is `6*x + 10`.

The full body, worked values, and file-level shape are defined once in [Code Conventions & Uniform Contract](../../architecture/code-conventions.md) and are not repeated here; the JSDoc applied uniformly to every function is described in [JSDoc conventions](../../guides/jsdoc-conventions.md).

## API Reference

The table below is generated from the JSDoc comments in `src/controllers/file_0.js` by `npm run docs:api` (`jsdoc2md`); its columns are the function, its parameter, and its return value.

<!-- BEGIN GENERATED API TABLE -->
**Total functions:** 1200

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `mod_0_0(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_2(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_3(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_4(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_5(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_6(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_7(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_8(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_9(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_10(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_11(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_12(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_13(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_14(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_15(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_16(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_17(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_18(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_19(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_20(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_21(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_22(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_23(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_24(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_25(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_26(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_27(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_28(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_29(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_30(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_31(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_32(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_33(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_34(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_35(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_36(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_37(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_38(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_39(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_40(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_41(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_42(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_43(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_44(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_45(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_46(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_47(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_48(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_49(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_50(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_51(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_52(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_53(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_54(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_55(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_56(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_57(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_58(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_59(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_60(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_61(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_62(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_63(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_64(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_65(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_66(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_67(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_68(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_69(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_70(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_71(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_72(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_73(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_74(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_75(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_76(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_77(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_78(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_79(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_80(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_81(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_82(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_83(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_84(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_85(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_86(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_87(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_88(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_89(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_90(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_91(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_92(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_93(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_94(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_95(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_96(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_97(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_98(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_99(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_100(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_101(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_102(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_103(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_104(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_105(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_106(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_107(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_108(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_109(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_110(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_111(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_112(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_113(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_114(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_115(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_116(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_117(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_118(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_119(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_120(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_121(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_122(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_123(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_124(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_125(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_126(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_127(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_128(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_129(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_130(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_131(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_132(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_133(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_134(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_135(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_136(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_137(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_138(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_139(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_140(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_141(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_142(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_143(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_144(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_145(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_146(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_147(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_148(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_149(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_150(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_151(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_152(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_153(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_154(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_155(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_156(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_157(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_158(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_159(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_160(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_161(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_162(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_163(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_164(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_165(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_166(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_167(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_168(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_169(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_170(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_171(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_172(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_173(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_174(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_175(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_176(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_177(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_178(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_179(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_180(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_181(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_182(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_183(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_184(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_185(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_186(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_187(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_188(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_189(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_190(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_191(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_192(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_193(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_194(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_195(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_196(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_197(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_198(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_199(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_200(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_201(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_202(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_203(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_204(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_205(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_206(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_207(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_208(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_209(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_210(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_211(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_212(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_213(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_214(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_215(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_216(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_217(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_218(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_219(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_220(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_221(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_222(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_223(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_224(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_225(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_226(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_227(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_228(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_229(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_230(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_231(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_232(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_233(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_234(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_235(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_236(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_237(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_238(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_239(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_240(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_241(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_242(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_243(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_244(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_245(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_246(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_247(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_248(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_249(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_250(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_251(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_252(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_253(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_254(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_255(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_256(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_257(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_258(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_259(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_260(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_261(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_262(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_263(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_264(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_265(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_266(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_267(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_268(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_269(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_270(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_271(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_272(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_273(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_274(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_275(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_276(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_277(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_278(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_279(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_280(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_281(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_282(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_283(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_284(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_285(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_286(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_287(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_288(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_289(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_290(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_291(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_292(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_293(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_294(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_295(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_296(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_297(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_298(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_299(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_300(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_301(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_302(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_303(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_304(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_305(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_306(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_307(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_308(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_309(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_310(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_311(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_312(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_313(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_314(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_315(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_316(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_317(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_318(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_319(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_320(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_321(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_322(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_323(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_324(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_325(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_326(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_327(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_328(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_329(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_330(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_331(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_332(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_333(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_334(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_335(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_336(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_337(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_338(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_339(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_340(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_341(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_342(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_343(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_344(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_345(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_346(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_347(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_348(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_349(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_350(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_351(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_352(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_353(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_354(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_355(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_356(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_357(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_358(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_359(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_360(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_361(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_362(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_363(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_364(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_365(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_366(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_367(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_368(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_369(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_370(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_371(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_372(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_373(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_374(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_375(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_376(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_377(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_378(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_379(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_380(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_381(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_382(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_383(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_384(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_385(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_386(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_387(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_388(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_389(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_390(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_391(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_392(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_393(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_394(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_395(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_396(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_397(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_398(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_399(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_400(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_401(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_402(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_403(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_404(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_405(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_406(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_407(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_408(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_409(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_410(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_411(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_412(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_413(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_414(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_415(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_416(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_417(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_418(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_419(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_420(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_421(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_422(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_423(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_424(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_425(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_426(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_427(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_428(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_429(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_430(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_431(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_432(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_433(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_434(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_435(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_436(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_437(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_438(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_439(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_440(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_441(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_442(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_443(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_444(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_445(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_446(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_447(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_448(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_449(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_450(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_451(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_452(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_453(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_454(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_455(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_456(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_457(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_458(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_459(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_460(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_461(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_462(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_463(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_464(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_465(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_466(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_467(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_468(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_469(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_470(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_471(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_472(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_473(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_474(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_475(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_476(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_477(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_478(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_479(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_480(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_481(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_482(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_483(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_484(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_485(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_486(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_487(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_488(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_489(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_490(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_491(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_492(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_493(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_494(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_495(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_496(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_497(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_498(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_499(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_500(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_501(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_502(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_503(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_504(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_505(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_506(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_507(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_508(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_509(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_510(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_511(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_512(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_513(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_514(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_515(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_516(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_517(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_518(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_519(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_520(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_521(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_522(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_523(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_524(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_525(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_526(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_527(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_528(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_529(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_530(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_531(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_532(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_533(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_534(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_535(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_536(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_537(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_538(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_539(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_540(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_541(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_542(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_543(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_544(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_545(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_546(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_547(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_548(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_549(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_550(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_551(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_552(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_553(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_554(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_555(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_556(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_557(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_558(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_559(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_560(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_561(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_562(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_563(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_564(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_565(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_566(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_567(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_568(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_569(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_570(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_571(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_572(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_573(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_574(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_575(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_576(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_577(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_578(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_579(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_580(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_581(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_582(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_583(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_584(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_585(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_586(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_587(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_588(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_589(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_590(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_591(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_592(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_593(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_594(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_595(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_596(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_597(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_598(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_599(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_600(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_601(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_602(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_603(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_604(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_605(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_606(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_607(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_608(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_609(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_610(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_611(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_612(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_613(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_614(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_615(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_616(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_617(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_618(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_619(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_620(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_621(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_622(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_623(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_624(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_625(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_626(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_627(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_628(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_629(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_630(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_631(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_632(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_633(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_634(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_635(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_636(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_637(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_638(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_639(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_640(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_641(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_642(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_643(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_644(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_645(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_646(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_647(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_648(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_649(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_650(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_651(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_652(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_653(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_654(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_655(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_656(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_657(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_658(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_659(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_660(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_661(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_662(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_663(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_664(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_665(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_666(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_667(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_668(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_669(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_670(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_671(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_672(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_673(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_674(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_675(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_676(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_677(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_678(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_679(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_680(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_681(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_682(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_683(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_684(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_685(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_686(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_687(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_688(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_689(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_690(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_691(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_692(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_693(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_694(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_695(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_696(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_697(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_698(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_699(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_700(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_701(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_702(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_703(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_704(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_705(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_706(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_707(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_708(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_709(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_710(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_711(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_712(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_713(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_714(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_715(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_716(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_717(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_718(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_719(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_720(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_721(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_722(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_723(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_724(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_725(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_726(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_727(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_728(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_729(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_730(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_731(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_732(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_733(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_734(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_735(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_736(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_737(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_738(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_739(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_740(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_741(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_742(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_743(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_744(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_745(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_746(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_747(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_748(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_749(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_750(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_751(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_752(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_753(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_754(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_755(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_756(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_757(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_758(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_759(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_760(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_761(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_762(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_763(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_764(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_765(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_766(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_767(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_768(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_769(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_770(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_771(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_772(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_773(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_774(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_775(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_776(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_777(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_778(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_779(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_780(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_781(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_782(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_783(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_784(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_785(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_786(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_787(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_788(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_789(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_790(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_791(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_792(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_793(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_794(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_795(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_796(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_797(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_798(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_799(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_800(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_801(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_802(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_803(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_804(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_805(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_806(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_807(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_808(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_809(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_810(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_811(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_812(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_813(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_814(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_815(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_816(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_817(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_818(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_819(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_820(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_821(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_822(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_823(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_824(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_825(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_826(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_827(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_828(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_829(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_830(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_831(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_832(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_833(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_834(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_835(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_836(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_837(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_838(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_839(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_840(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_841(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_842(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_843(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_844(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_845(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_846(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_847(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_848(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_849(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_850(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_851(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_852(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_853(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_854(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_855(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_856(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_857(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_858(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_859(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_860(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_861(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_862(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_863(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_864(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_865(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_866(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_867(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_868(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_869(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_870(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_871(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_872(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_873(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_874(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_875(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_876(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_877(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_878(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_879(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_880(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_881(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_882(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_883(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_884(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_885(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_886(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_887(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_888(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_889(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_890(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_891(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_892(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_893(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_894(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_895(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_896(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_897(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_898(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_899(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_900(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_901(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_902(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_903(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_904(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_905(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_906(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_907(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_908(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_909(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_910(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_911(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_912(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_913(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_914(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_915(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_916(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_917(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_918(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_919(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_920(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_921(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_922(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_923(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_924(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_925(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_926(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_927(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_928(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_929(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_930(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_931(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_932(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_933(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_934(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_935(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_936(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_937(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_938(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_939(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_940(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_941(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_942(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_943(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_944(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_945(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_946(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_947(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_948(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_949(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_950(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_951(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_952(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_953(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_954(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_955(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_956(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_957(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_958(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_959(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_960(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_961(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_962(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_963(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_964(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_965(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_966(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_967(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_968(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_969(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_970(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_971(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_972(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_973(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_974(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_975(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_976(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_977(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_978(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_979(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_980(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_981(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_982(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_983(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_984(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_985(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_986(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_987(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_988(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_989(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_990(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_991(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_992(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_993(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_994(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_995(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_996(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_997(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_998(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_999(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1000(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1001(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1002(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1003(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1004(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1005(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1006(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1007(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1008(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1009(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1010(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1011(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1012(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1013(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1014(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1015(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1016(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1017(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1018(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1019(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1020(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1021(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1022(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1023(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1024(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1025(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1026(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1027(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1028(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1029(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1030(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1031(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1032(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1033(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1034(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1035(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1036(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1037(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1038(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1039(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1040(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1041(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1042(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1043(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1044(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1045(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1046(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1047(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1048(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1049(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1050(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1051(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1052(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1053(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1054(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1055(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1056(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1057(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1058(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1059(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1060(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1061(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1062(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1063(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1064(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1065(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1066(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1067(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1068(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1069(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1070(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1071(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1072(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1073(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1074(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1075(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1076(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1077(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1078(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1079(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1080(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1081(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1082(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1083(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1084(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1085(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1086(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1087(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1088(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1089(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1090(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1091(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1092(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1093(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1094(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1095(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1096(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1097(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1098(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1099(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1100(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1101(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1102(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1103(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1104(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1105(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1106(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1107(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1108(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1109(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1110(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1111(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1112(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1113(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1114(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1115(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1116(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1117(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1118(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1119(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1120(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1121(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1122(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1123(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1124(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1125(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1126(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1127(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1128(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1129(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1130(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1131(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1132(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1133(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1134(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1135(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1136(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1137(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1138(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1139(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1140(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1141(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1142(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1143(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1144(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1145(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1146(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1147(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1148(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1149(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1150(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1151(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1152(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1153(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1154(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1155(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1156(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1157(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1158(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1159(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1160(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1161(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1162(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1163(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1164(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1165(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1166(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1167(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1168(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1169(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1170(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1171(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1172(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1173(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1174(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1175(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1176(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1177(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1178(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1179(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1180(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1181(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1182(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1183(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1184(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1185(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1186(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1187(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1188(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1189(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1190(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1191(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1192(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1193(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1194(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1195(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1196(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1197(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1198(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_0_1199(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
<!-- END GENERATED API TABLE -->

## Example

```javascript
// Each function returns 6*x + 10 for integer x.
mod_0_0(5); // => 40   (6*5 + 10)
mod_0_0(1); // => 16   (6*1 + 10)
```

## Source Citation

Source: src/controllers/file_0.js:L3-L10
