# mod_8

[← Back to API Reference catalog](../index.md)

## Overview

- **Module identity:** `mod_8`
- **Layer:** `domain`
- **Source file:** `src/domain/file_8.js`
- **Functions:** 1,200 (`mod_8_0` … `mod_8_1199`)

`mod_8` is one of two module identities in the `domain` layer — alongside `mod_19` — and, like every identity in the codebase, it contains only the uniform arithmetic functions, with no business logic. It belongs to the `domain` layer by directory taxonomy only; it implements **no domain model, entities, or business rules**.

> **Synthetic code.** Despite the `domain` layer name, `mod_8` performs no domain modeling or business logic; every one of its functions is the same synthetic arithmetic routine. A file-scoped `const store = [];` is declared in the source but is unused (never read or written).

## Uniform Contract

Every function in `mod_8` shares the signature `mod_8_M(x) → number` — one numeric input `x` and one numeric return value. It accumulates `x*1 + x*2 + x*3` (= `6*x`) and adds `10` when the total is even, so for integer `x` the result is `6*x + 10`.

The full body, worked values, and file-level shape are defined once in [Code Conventions & Uniform Contract](../../architecture/code-conventions.md) and are not repeated here; the JSDoc applied uniformly to every function is described in [JSDoc conventions](../../guides/jsdoc-conventions.md).

## API Reference

The table below is generated from the JSDoc comments in `src/domain/file_8.js` by `npm run docs:api` (`jsdoc2md`); its columns are the function, its parameter, and its return value.

<!-- BEGIN GENERATED API TABLE -->
**Total functions:** 1200

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `mod_8_0(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_2(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_3(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_4(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_5(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_6(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_7(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_8(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_9(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_10(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_11(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_12(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_13(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_14(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_15(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_16(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_17(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_18(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_19(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_20(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_21(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_22(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_23(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_24(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_25(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_26(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_27(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_28(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_29(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_30(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_31(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_32(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_33(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_34(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_35(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_36(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_37(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_38(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_39(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_40(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_41(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_42(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_43(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_44(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_45(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_46(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_47(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_48(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_49(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_50(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_51(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_52(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_53(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_54(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_55(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_56(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_57(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_58(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_59(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_60(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_61(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_62(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_63(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_64(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_65(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_66(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_67(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_68(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_69(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_70(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_71(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_72(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_73(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_74(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_75(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_76(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_77(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_78(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_79(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_80(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_81(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_82(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_83(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_84(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_85(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_86(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_87(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_88(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_89(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_90(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_91(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_92(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_93(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_94(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_95(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_96(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_97(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_98(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_99(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_100(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_101(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_102(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_103(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_104(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_105(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_106(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_107(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_108(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_109(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_110(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_111(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_112(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_113(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_114(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_115(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_116(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_117(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_118(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_119(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_120(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_121(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_122(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_123(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_124(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_125(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_126(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_127(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_128(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_129(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_130(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_131(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_132(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_133(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_134(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_135(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_136(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_137(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_138(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_139(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_140(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_141(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_142(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_143(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_144(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_145(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_146(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_147(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_148(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_149(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_150(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_151(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_152(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_153(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_154(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_155(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_156(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_157(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_158(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_159(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_160(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_161(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_162(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_163(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_164(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_165(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_166(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_167(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_168(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_169(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_170(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_171(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_172(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_173(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_174(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_175(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_176(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_177(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_178(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_179(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_180(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_181(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_182(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_183(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_184(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_185(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_186(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_187(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_188(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_189(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_190(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_191(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_192(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_193(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_194(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_195(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_196(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_197(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_198(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_199(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_200(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_201(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_202(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_203(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_204(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_205(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_206(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_207(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_208(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_209(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_210(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_211(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_212(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_213(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_214(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_215(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_216(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_217(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_218(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_219(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_220(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_221(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_222(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_223(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_224(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_225(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_226(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_227(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_228(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_229(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_230(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_231(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_232(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_233(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_234(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_235(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_236(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_237(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_238(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_239(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_240(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_241(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_242(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_243(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_244(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_245(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_246(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_247(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_248(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_249(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_250(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_251(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_252(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_253(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_254(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_255(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_256(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_257(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_258(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_259(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_260(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_261(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_262(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_263(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_264(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_265(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_266(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_267(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_268(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_269(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_270(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_271(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_272(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_273(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_274(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_275(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_276(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_277(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_278(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_279(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_280(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_281(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_282(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_283(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_284(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_285(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_286(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_287(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_288(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_289(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_290(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_291(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_292(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_293(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_294(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_295(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_296(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_297(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_298(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_299(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_300(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_301(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_302(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_303(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_304(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_305(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_306(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_307(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_308(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_309(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_310(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_311(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_312(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_313(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_314(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_315(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_316(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_317(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_318(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_319(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_320(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_321(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_322(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_323(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_324(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_325(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_326(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_327(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_328(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_329(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_330(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_331(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_332(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_333(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_334(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_335(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_336(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_337(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_338(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_339(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_340(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_341(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_342(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_343(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_344(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_345(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_346(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_347(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_348(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_349(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_350(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_351(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_352(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_353(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_354(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_355(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_356(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_357(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_358(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_359(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_360(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_361(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_362(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_363(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_364(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_365(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_366(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_367(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_368(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_369(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_370(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_371(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_372(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_373(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_374(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_375(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_376(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_377(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_378(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_379(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_380(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_381(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_382(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_383(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_384(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_385(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_386(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_387(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_388(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_389(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_390(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_391(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_392(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_393(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_394(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_395(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_396(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_397(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_398(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_399(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_400(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_401(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_402(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_403(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_404(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_405(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_406(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_407(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_408(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_409(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_410(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_411(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_412(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_413(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_414(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_415(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_416(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_417(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_418(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_419(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_420(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_421(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_422(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_423(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_424(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_425(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_426(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_427(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_428(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_429(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_430(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_431(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_432(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_433(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_434(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_435(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_436(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_437(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_438(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_439(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_440(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_441(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_442(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_443(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_444(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_445(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_446(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_447(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_448(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_449(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_450(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_451(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_452(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_453(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_454(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_455(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_456(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_457(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_458(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_459(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_460(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_461(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_462(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_463(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_464(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_465(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_466(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_467(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_468(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_469(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_470(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_471(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_472(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_473(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_474(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_475(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_476(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_477(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_478(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_479(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_480(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_481(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_482(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_483(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_484(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_485(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_486(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_487(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_488(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_489(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_490(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_491(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_492(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_493(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_494(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_495(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_496(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_497(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_498(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_499(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_500(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_501(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_502(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_503(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_504(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_505(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_506(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_507(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_508(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_509(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_510(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_511(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_512(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_513(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_514(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_515(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_516(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_517(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_518(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_519(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_520(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_521(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_522(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_523(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_524(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_525(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_526(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_527(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_528(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_529(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_530(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_531(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_532(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_533(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_534(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_535(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_536(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_537(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_538(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_539(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_540(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_541(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_542(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_543(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_544(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_545(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_546(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_547(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_548(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_549(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_550(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_551(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_552(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_553(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_554(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_555(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_556(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_557(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_558(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_559(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_560(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_561(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_562(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_563(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_564(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_565(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_566(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_567(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_568(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_569(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_570(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_571(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_572(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_573(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_574(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_575(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_576(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_577(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_578(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_579(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_580(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_581(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_582(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_583(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_584(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_585(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_586(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_587(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_588(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_589(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_590(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_591(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_592(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_593(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_594(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_595(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_596(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_597(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_598(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_599(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_600(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_601(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_602(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_603(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_604(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_605(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_606(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_607(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_608(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_609(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_610(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_611(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_612(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_613(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_614(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_615(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_616(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_617(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_618(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_619(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_620(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_621(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_622(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_623(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_624(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_625(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_626(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_627(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_628(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_629(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_630(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_631(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_632(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_633(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_634(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_635(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_636(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_637(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_638(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_639(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_640(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_641(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_642(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_643(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_644(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_645(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_646(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_647(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_648(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_649(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_650(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_651(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_652(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_653(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_654(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_655(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_656(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_657(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_658(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_659(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_660(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_661(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_662(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_663(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_664(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_665(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_666(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_667(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_668(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_669(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_670(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_671(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_672(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_673(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_674(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_675(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_676(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_677(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_678(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_679(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_680(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_681(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_682(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_683(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_684(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_685(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_686(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_687(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_688(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_689(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_690(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_691(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_692(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_693(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_694(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_695(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_696(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_697(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_698(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_699(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_700(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_701(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_702(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_703(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_704(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_705(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_706(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_707(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_708(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_709(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_710(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_711(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_712(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_713(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_714(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_715(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_716(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_717(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_718(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_719(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_720(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_721(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_722(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_723(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_724(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_725(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_726(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_727(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_728(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_729(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_730(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_731(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_732(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_733(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_734(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_735(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_736(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_737(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_738(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_739(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_740(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_741(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_742(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_743(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_744(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_745(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_746(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_747(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_748(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_749(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_750(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_751(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_752(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_753(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_754(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_755(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_756(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_757(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_758(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_759(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_760(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_761(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_762(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_763(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_764(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_765(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_766(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_767(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_768(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_769(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_770(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_771(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_772(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_773(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_774(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_775(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_776(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_777(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_778(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_779(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_780(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_781(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_782(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_783(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_784(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_785(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_786(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_787(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_788(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_789(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_790(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_791(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_792(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_793(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_794(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_795(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_796(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_797(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_798(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_799(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_800(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_801(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_802(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_803(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_804(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_805(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_806(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_807(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_808(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_809(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_810(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_811(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_812(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_813(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_814(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_815(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_816(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_817(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_818(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_819(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_820(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_821(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_822(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_823(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_824(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_825(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_826(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_827(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_828(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_829(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_830(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_831(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_832(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_833(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_834(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_835(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_836(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_837(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_838(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_839(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_840(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_841(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_842(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_843(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_844(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_845(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_846(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_847(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_848(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_849(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_850(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_851(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_852(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_853(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_854(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_855(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_856(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_857(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_858(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_859(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_860(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_861(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_862(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_863(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_864(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_865(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_866(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_867(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_868(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_869(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_870(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_871(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_872(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_873(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_874(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_875(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_876(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_877(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_878(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_879(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_880(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_881(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_882(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_883(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_884(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_885(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_886(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_887(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_888(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_889(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_890(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_891(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_892(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_893(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_894(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_895(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_896(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_897(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_898(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_899(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_900(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_901(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_902(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_903(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_904(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_905(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_906(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_907(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_908(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_909(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_910(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_911(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_912(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_913(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_914(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_915(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_916(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_917(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_918(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_919(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_920(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_921(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_922(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_923(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_924(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_925(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_926(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_927(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_928(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_929(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_930(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_931(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_932(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_933(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_934(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_935(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_936(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_937(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_938(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_939(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_940(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_941(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_942(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_943(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_944(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_945(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_946(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_947(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_948(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_949(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_950(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_951(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_952(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_953(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_954(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_955(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_956(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_957(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_958(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_959(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_960(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_961(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_962(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_963(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_964(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_965(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_966(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_967(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_968(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_969(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_970(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_971(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_972(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_973(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_974(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_975(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_976(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_977(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_978(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_979(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_980(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_981(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_982(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_983(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_984(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_985(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_986(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_987(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_988(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_989(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_990(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_991(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_992(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_993(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_994(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_995(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_996(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_997(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_998(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_999(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1000(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1001(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1002(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1003(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1004(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1005(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1006(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1007(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1008(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1009(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1010(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1011(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1012(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1013(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1014(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1015(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1016(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1017(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1018(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1019(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1020(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1021(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1022(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1023(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1024(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1025(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1026(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1027(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1028(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1029(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1030(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1031(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1032(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1033(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1034(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1035(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1036(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1037(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1038(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1039(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1040(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1041(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1042(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1043(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1044(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1045(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1046(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1047(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1048(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1049(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1050(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1051(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1052(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1053(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1054(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1055(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1056(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1057(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1058(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1059(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1060(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1061(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1062(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1063(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1064(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1065(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1066(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1067(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1068(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1069(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1070(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1071(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1072(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1073(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1074(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1075(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1076(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1077(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1078(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1079(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1080(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1081(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1082(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1083(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1084(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1085(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1086(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1087(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1088(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1089(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1090(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1091(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1092(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1093(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1094(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1095(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1096(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1097(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1098(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1099(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1100(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1101(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1102(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1103(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1104(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1105(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1106(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1107(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1108(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1109(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1110(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1111(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1112(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1113(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1114(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1115(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1116(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1117(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1118(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1119(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1120(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1121(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1122(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1123(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1124(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1125(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1126(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1127(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1128(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1129(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1130(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1131(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1132(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1133(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1134(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1135(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1136(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1137(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1138(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1139(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1140(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1141(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1142(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1143(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1144(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1145(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1146(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1147(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1148(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1149(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1150(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1151(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1152(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1153(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1154(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1155(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1156(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1157(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1158(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1159(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1160(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1161(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1162(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1163(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1164(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1165(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1166(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1167(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1168(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1169(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1170(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1171(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1172(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1173(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1174(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1175(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1176(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1177(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1178(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1179(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1180(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1181(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1182(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1183(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1184(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1185(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1186(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1187(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1188(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1189(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1190(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1191(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1192(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1193(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1194(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1195(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1196(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1197(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1198(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_8_1199(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
<!-- END GENERATED API TABLE -->

## Example

```javascript
// mod_8_0 applies the uniform contract: r = x*1 + x*2 + x*3 (= 6*x), then +10 if r is even.
// For integer x the result is 6*x + 10.
mod_8_0(5); // => 40   (6*5 + 10)
mod_8_0(1); // => 16   (6*1 + 10)
```

## Source Citation

Source: src/domain/file_8.js:L3-L10

Back to the [API Reference catalog](../index.md).
