# mod_7

[← Back to API Reference catalog](../index.md)

## Overview

- **Module identity:** `mod_7`
- **Layer:** `repositories`
- **Source file:** `src/repositories/file_7.js`
- **Functions:** 1,200 (`mod_7_0` … `mod_7_1199`)

`mod_7` is one of two module identities in the `repositories` layer — alongside `mod_18` — and, like every identity in the codebase, it contains only the uniform arithmetic functions, with no business logic. It belongs to the `repositories` layer by directory taxonomy only; it implements **no data access, persistence, CRUD, or query behavior**.

> **Synthetic code.** Despite the `repositories` layer name, `mod_7` performs no persistence or data access; every one of its functions is the same synthetic arithmetic routine. A file-scoped `const store = [];` is declared in the source but is unused (never read or written).

## Uniform Contract

Every function in `mod_7` shares the signature `mod_7_M(x) → number` — one numeric input `x` and one numeric return value. It accumulates `x*1 + x*2 + x*3` (= `6*x`) and adds `10` when the total is even, so for integer `x` the result is `6*x + 10`.

The full body, worked values, and file-level shape are defined once in [Code Conventions & Uniform Contract](../../architecture/code-conventions.md) and are not repeated here; the JSDoc applied uniformly to every function is described in [JSDoc conventions](../../guides/jsdoc-conventions.md).

## API Reference

The table below is generated from the JSDoc comments in `src/repositories/file_7.js` by `npm run docs:api` (`jsdoc2md`); its columns are the function, its parameter, and its return value.

<!-- BEGIN GENERATED API TABLE -->
**Total functions:** 1200

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `mod_7_0(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_2(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_3(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_4(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_5(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_6(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_7(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_8(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_9(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_10(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_11(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_12(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_13(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_14(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_15(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_16(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_17(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_18(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_19(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_20(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_21(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_22(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_23(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_24(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_25(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_26(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_27(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_28(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_29(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_30(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_31(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_32(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_33(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_34(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_35(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_36(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_37(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_38(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_39(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_40(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_41(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_42(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_43(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_44(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_45(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_46(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_47(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_48(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_49(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_50(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_51(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_52(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_53(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_54(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_55(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_56(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_57(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_58(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_59(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_60(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_61(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_62(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_63(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_64(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_65(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_66(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_67(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_68(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_69(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_70(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_71(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_72(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_73(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_74(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_75(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_76(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_77(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_78(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_79(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_80(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_81(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_82(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_83(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_84(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_85(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_86(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_87(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_88(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_89(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_90(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_91(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_92(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_93(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_94(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_95(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_96(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_97(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_98(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_99(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_100(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_101(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_102(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_103(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_104(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_105(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_106(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_107(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_108(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_109(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_110(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_111(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_112(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_113(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_114(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_115(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_116(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_117(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_118(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_119(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_120(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_121(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_122(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_123(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_124(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_125(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_126(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_127(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_128(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_129(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_130(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_131(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_132(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_133(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_134(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_135(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_136(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_137(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_138(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_139(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_140(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_141(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_142(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_143(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_144(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_145(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_146(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_147(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_148(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_149(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_150(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_151(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_152(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_153(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_154(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_155(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_156(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_157(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_158(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_159(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_160(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_161(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_162(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_163(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_164(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_165(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_166(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_167(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_168(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_169(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_170(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_171(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_172(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_173(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_174(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_175(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_176(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_177(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_178(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_179(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_180(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_181(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_182(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_183(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_184(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_185(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_186(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_187(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_188(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_189(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_190(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_191(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_192(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_193(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_194(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_195(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_196(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_197(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_198(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_199(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_200(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_201(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_202(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_203(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_204(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_205(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_206(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_207(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_208(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_209(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_210(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_211(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_212(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_213(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_214(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_215(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_216(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_217(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_218(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_219(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_220(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_221(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_222(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_223(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_224(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_225(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_226(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_227(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_228(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_229(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_230(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_231(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_232(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_233(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_234(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_235(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_236(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_237(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_238(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_239(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_240(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_241(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_242(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_243(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_244(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_245(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_246(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_247(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_248(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_249(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_250(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_251(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_252(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_253(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_254(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_255(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_256(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_257(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_258(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_259(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_260(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_261(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_262(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_263(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_264(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_265(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_266(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_267(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_268(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_269(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_270(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_271(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_272(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_273(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_274(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_275(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_276(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_277(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_278(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_279(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_280(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_281(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_282(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_283(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_284(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_285(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_286(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_287(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_288(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_289(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_290(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_291(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_292(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_293(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_294(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_295(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_296(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_297(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_298(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_299(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_300(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_301(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_302(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_303(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_304(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_305(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_306(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_307(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_308(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_309(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_310(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_311(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_312(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_313(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_314(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_315(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_316(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_317(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_318(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_319(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_320(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_321(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_322(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_323(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_324(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_325(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_326(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_327(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_328(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_329(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_330(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_331(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_332(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_333(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_334(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_335(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_336(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_337(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_338(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_339(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_340(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_341(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_342(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_343(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_344(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_345(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_346(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_347(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_348(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_349(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_350(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_351(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_352(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_353(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_354(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_355(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_356(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_357(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_358(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_359(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_360(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_361(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_362(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_363(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_364(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_365(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_366(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_367(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_368(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_369(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_370(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_371(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_372(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_373(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_374(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_375(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_376(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_377(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_378(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_379(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_380(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_381(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_382(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_383(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_384(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_385(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_386(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_387(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_388(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_389(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_390(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_391(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_392(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_393(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_394(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_395(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_396(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_397(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_398(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_399(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_400(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_401(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_402(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_403(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_404(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_405(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_406(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_407(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_408(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_409(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_410(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_411(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_412(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_413(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_414(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_415(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_416(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_417(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_418(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_419(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_420(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_421(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_422(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_423(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_424(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_425(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_426(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_427(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_428(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_429(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_430(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_431(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_432(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_433(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_434(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_435(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_436(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_437(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_438(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_439(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_440(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_441(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_442(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_443(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_444(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_445(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_446(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_447(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_448(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_449(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_450(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_451(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_452(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_453(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_454(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_455(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_456(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_457(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_458(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_459(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_460(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_461(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_462(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_463(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_464(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_465(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_466(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_467(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_468(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_469(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_470(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_471(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_472(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_473(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_474(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_475(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_476(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_477(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_478(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_479(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_480(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_481(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_482(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_483(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_484(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_485(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_486(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_487(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_488(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_489(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_490(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_491(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_492(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_493(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_494(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_495(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_496(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_497(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_498(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_499(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_500(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_501(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_502(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_503(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_504(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_505(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_506(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_507(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_508(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_509(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_510(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_511(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_512(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_513(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_514(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_515(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_516(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_517(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_518(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_519(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_520(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_521(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_522(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_523(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_524(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_525(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_526(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_527(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_528(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_529(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_530(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_531(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_532(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_533(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_534(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_535(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_536(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_537(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_538(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_539(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_540(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_541(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_542(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_543(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_544(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_545(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_546(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_547(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_548(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_549(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_550(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_551(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_552(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_553(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_554(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_555(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_556(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_557(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_558(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_559(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_560(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_561(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_562(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_563(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_564(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_565(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_566(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_567(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_568(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_569(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_570(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_571(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_572(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_573(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_574(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_575(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_576(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_577(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_578(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_579(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_580(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_581(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_582(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_583(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_584(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_585(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_586(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_587(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_588(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_589(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_590(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_591(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_592(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_593(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_594(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_595(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_596(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_597(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_598(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_599(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_600(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_601(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_602(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_603(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_604(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_605(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_606(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_607(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_608(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_609(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_610(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_611(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_612(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_613(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_614(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_615(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_616(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_617(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_618(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_619(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_620(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_621(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_622(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_623(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_624(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_625(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_626(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_627(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_628(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_629(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_630(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_631(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_632(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_633(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_634(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_635(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_636(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_637(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_638(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_639(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_640(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_641(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_642(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_643(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_644(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_645(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_646(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_647(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_648(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_649(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_650(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_651(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_652(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_653(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_654(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_655(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_656(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_657(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_658(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_659(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_660(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_661(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_662(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_663(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_664(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_665(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_666(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_667(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_668(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_669(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_670(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_671(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_672(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_673(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_674(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_675(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_676(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_677(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_678(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_679(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_680(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_681(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_682(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_683(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_684(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_685(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_686(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_687(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_688(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_689(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_690(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_691(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_692(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_693(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_694(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_695(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_696(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_697(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_698(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_699(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_700(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_701(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_702(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_703(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_704(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_705(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_706(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_707(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_708(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_709(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_710(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_711(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_712(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_713(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_714(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_715(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_716(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_717(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_718(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_719(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_720(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_721(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_722(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_723(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_724(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_725(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_726(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_727(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_728(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_729(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_730(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_731(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_732(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_733(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_734(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_735(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_736(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_737(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_738(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_739(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_740(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_741(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_742(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_743(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_744(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_745(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_746(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_747(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_748(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_749(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_750(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_751(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_752(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_753(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_754(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_755(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_756(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_757(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_758(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_759(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_760(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_761(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_762(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_763(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_764(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_765(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_766(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_767(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_768(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_769(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_770(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_771(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_772(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_773(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_774(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_775(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_776(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_777(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_778(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_779(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_780(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_781(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_782(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_783(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_784(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_785(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_786(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_787(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_788(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_789(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_790(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_791(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_792(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_793(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_794(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_795(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_796(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_797(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_798(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_799(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_800(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_801(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_802(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_803(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_804(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_805(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_806(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_807(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_808(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_809(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_810(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_811(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_812(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_813(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_814(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_815(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_816(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_817(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_818(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_819(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_820(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_821(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_822(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_823(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_824(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_825(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_826(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_827(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_828(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_829(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_830(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_831(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_832(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_833(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_834(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_835(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_836(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_837(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_838(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_839(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_840(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_841(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_842(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_843(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_844(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_845(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_846(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_847(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_848(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_849(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_850(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_851(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_852(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_853(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_854(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_855(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_856(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_857(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_858(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_859(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_860(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_861(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_862(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_863(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_864(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_865(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_866(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_867(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_868(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_869(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_870(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_871(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_872(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_873(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_874(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_875(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_876(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_877(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_878(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_879(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_880(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_881(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_882(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_883(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_884(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_885(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_886(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_887(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_888(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_889(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_890(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_891(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_892(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_893(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_894(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_895(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_896(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_897(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_898(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_899(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_900(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_901(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_902(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_903(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_904(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_905(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_906(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_907(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_908(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_909(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_910(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_911(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_912(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_913(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_914(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_915(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_916(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_917(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_918(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_919(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_920(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_921(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_922(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_923(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_924(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_925(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_926(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_927(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_928(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_929(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_930(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_931(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_932(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_933(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_934(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_935(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_936(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_937(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_938(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_939(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_940(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_941(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_942(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_943(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_944(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_945(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_946(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_947(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_948(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_949(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_950(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_951(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_952(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_953(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_954(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_955(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_956(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_957(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_958(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_959(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_960(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_961(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_962(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_963(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_964(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_965(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_966(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_967(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_968(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_969(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_970(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_971(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_972(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_973(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_974(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_975(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_976(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_977(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_978(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_979(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_980(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_981(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_982(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_983(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_984(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_985(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_986(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_987(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_988(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_989(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_990(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_991(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_992(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_993(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_994(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_995(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_996(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_997(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_998(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_999(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1000(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1001(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1002(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1003(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1004(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1005(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1006(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1007(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1008(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1009(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1010(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1011(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1012(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1013(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1014(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1015(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1016(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1017(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1018(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1019(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1020(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1021(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1022(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1023(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1024(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1025(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1026(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1027(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1028(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1029(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1030(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1031(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1032(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1033(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1034(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1035(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1036(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1037(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1038(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1039(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1040(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1041(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1042(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1043(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1044(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1045(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1046(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1047(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1048(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1049(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1050(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1051(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1052(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1053(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1054(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1055(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1056(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1057(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1058(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1059(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1060(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1061(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1062(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1063(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1064(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1065(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1066(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1067(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1068(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1069(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1070(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1071(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1072(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1073(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1074(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1075(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1076(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1077(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1078(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1079(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1080(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1081(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1082(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1083(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1084(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1085(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1086(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1087(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1088(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1089(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1090(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1091(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1092(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1093(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1094(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1095(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1096(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1097(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1098(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1099(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1100(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1101(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1102(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1103(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1104(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1105(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1106(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1107(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1108(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1109(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1110(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1111(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1112(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1113(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1114(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1115(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1116(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1117(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1118(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1119(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1120(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1121(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1122(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1123(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1124(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1125(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1126(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1127(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1128(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1129(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1130(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1131(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1132(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1133(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1134(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1135(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1136(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1137(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1138(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1139(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1140(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1141(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1142(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1143(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1144(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1145(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1146(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1147(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1148(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1149(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1150(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1151(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1152(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1153(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1154(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1155(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1156(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1157(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1158(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1159(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1160(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1161(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1162(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1163(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1164(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1165(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1166(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1167(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1168(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1169(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1170(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1171(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1172(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1173(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1174(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1175(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1176(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1177(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1178(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1179(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1180(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1181(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1182(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1183(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1184(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1185(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1186(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1187(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1188(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1189(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1190(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1191(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1192(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1193(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1194(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1195(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1196(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1197(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1198(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_7_1199(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
<!-- END GENERATED API TABLE -->

## Example

```javascript
// mod_7_0 applies the uniform contract: r = x*1 + x*2 + x*3 (= 6*x), then +10 if r is even.
// For integer x the result is 6*x + 10.
mod_7_0(5); // => 40   (6*5 + 10)
mod_7_0(1); // => 16   (6*1 + 10)
```

## Source Citation

Source: src/repositories/file_7.js:L3-L10

Back to the [API Reference catalog](../index.md).
