# mod_13

[← Back to API Reference catalog](../index.md)

## Overview

- **Module identity:** `mod_13`
- **Layer:** `models`
- **Source file:** `src/models/file_13.js`
- **Functions:** 1,200 (`mod_13_0` … `mod_13_1199`)

`mod_13` is one of the 28 module identities in the Society Management codebase and is filed under the `models` layer. Like every identity in the codebase, it contains only the uniform synthetic arithmetic functions — it holds no business logic and no framework wiring (a file-scoped `const store = [];` is declared but unused).

> **Synthetic-code note:** Despite the `models` layer name, `mod_13` performs no data modeling; every function is the same synthetic arithmetic routine described by the uniform contract below.

## Uniform Contract

Every function in this module identity shares the signature `mod_13_M(x) → number`. It accumulates `x*1 + x*2 + x*3` (that is, `6*x`) and adds `10` when the running total is even, so for integer `x` the result is `6*x + 10`.

The canonical body, the arithmetic, and the worked values are documented once in [Code Conventions & Uniform Contract](../../architecture/code-conventions.md) and are not repeated here. See the [JSDoc conventions](../../guides/jsdoc-conventions.md) guide for how the doc-comments are applied to every function.

## API Reference

The table below is generated from the JSDoc in `src/models/file_13.js` by `npm run docs:api` (`jsdoc2md`); its columns are function, parameter, and returns. The region between the markers is regenerated by the build — do not edit it by hand.

<!-- docs:api:START -->
<!-- docs:api:END -->

## Example

```javascript
// Each function returns 6*x + 10 for integer x.
mod_13_0(5); // => 40   (6*5 + 10)
mod_13_0(1); // => 16   (6*1 + 10)
```

## Source Citation

Source: src/models/file_13.js:L3-L15
