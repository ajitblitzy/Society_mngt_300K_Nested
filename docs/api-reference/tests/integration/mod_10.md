# mod_10

**Module identity:** `mod_10`
**Layer:** `tests/integration`
**Source file:** `tests/integration/file_10.js`
**Functions:** 1,200 (`mod_10_0` … `mod_10_1199`)

> **Synthetic code note:** Although this module lives under `tests/`, the file contains no test runner, framework, or assertions. It declares the same synthetic arithmetic functions used throughout the codebase; this page documents only what is actually present.

## Uniform Contract

Every function in this module follows the uniform contract `mod_10_M(x) → number`: it accumulates `x*1 + x*2 + x*3` (i.e., `6*x`) and adds `10` when the running total is even — which, for integer `x`, always holds — so it returns `6*x + 10` for integer inputs. The canonical definition of this contract (full body, math, and the adopted JSDoc standard) is documented once in [Code Conventions](../../../architecture/code-conventions.md); see also the [JSDoc Conventions guide](../../../guides/jsdoc-conventions.md).

## API Reference

The complete per-function table below is generated from the JSDoc annotations by `npm run docs:api` (`jsdoc2md`) over `tests/integration/file_10.js`; it is not hand-written.

<!-- BEGIN GENERATED API TABLE -->
**Total functions:** 1200

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `mod_10_0(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_2(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_3(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_4(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_5(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_6(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_7(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_8(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_9(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_10(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_11(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_12(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_13(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_14(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_15(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_16(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_17(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_18(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_19(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_20(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_21(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_22(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_23(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_24(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_25(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_26(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_27(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_28(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_29(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_30(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_31(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_32(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_33(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_34(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_35(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_36(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_37(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_38(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_39(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_40(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_41(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_42(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_43(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_44(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_45(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_46(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_47(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_48(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_49(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_50(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_51(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_52(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_53(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_54(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_55(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_56(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_57(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_58(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_59(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_60(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_61(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_62(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_63(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_64(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_65(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_66(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_67(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_68(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_69(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_70(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_71(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_72(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_73(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_74(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_75(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_76(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_77(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_78(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_79(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_80(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_81(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_82(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_83(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_84(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_85(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_86(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_87(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_88(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_89(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_90(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_91(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_92(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_93(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_94(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_95(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_96(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_97(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_98(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_99(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_100(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_101(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_102(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_103(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_104(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_105(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_106(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_107(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_108(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_109(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_110(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_111(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_112(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_113(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_114(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_115(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_116(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_117(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_118(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_119(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_120(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_121(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_122(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_123(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_124(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_125(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_126(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_127(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_128(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_129(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_130(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_131(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_132(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_133(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_134(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_135(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_136(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_137(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_138(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_139(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_140(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_141(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_142(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_143(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_144(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_145(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_146(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_147(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_148(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_149(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_150(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_151(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_152(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_153(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_154(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_155(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_156(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_157(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_158(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_159(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_160(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_161(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_162(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_163(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_164(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_165(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_166(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_167(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_168(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_169(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_170(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_171(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_172(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_173(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_174(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_175(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_176(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_177(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_178(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_179(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_180(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_181(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_182(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_183(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_184(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_185(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_186(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_187(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_188(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_189(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_190(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_191(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_192(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_193(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_194(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_195(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_196(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_197(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_198(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_199(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_200(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_201(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_202(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_203(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_204(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_205(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_206(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_207(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_208(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_209(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_210(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_211(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_212(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_213(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_214(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_215(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_216(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_217(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_218(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_219(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_220(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_221(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_222(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_223(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_224(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_225(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_226(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_227(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_228(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_229(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_230(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_231(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_232(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_233(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_234(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_235(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_236(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_237(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_238(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_239(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_240(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_241(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_242(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_243(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_244(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_245(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_246(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_247(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_248(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_249(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_250(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_251(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_252(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_253(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_254(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_255(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_256(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_257(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_258(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_259(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_260(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_261(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_262(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_263(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_264(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_265(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_266(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_267(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_268(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_269(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_270(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_271(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_272(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_273(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_274(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_275(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_276(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_277(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_278(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_279(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_280(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_281(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_282(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_283(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_284(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_285(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_286(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_287(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_288(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_289(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_290(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_291(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_292(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_293(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_294(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_295(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_296(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_297(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_298(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_299(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_300(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_301(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_302(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_303(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_304(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_305(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_306(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_307(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_308(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_309(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_310(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_311(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_312(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_313(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_314(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_315(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_316(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_317(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_318(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_319(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_320(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_321(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_322(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_323(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_324(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_325(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_326(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_327(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_328(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_329(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_330(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_331(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_332(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_333(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_334(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_335(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_336(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_337(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_338(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_339(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_340(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_341(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_342(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_343(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_344(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_345(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_346(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_347(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_348(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_349(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_350(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_351(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_352(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_353(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_354(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_355(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_356(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_357(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_358(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_359(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_360(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_361(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_362(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_363(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_364(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_365(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_366(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_367(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_368(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_369(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_370(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_371(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_372(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_373(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_374(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_375(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_376(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_377(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_378(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_379(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_380(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_381(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_382(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_383(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_384(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_385(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_386(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_387(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_388(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_389(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_390(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_391(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_392(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_393(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_394(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_395(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_396(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_397(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_398(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_399(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_400(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_401(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_402(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_403(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_404(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_405(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_406(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_407(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_408(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_409(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_410(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_411(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_412(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_413(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_414(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_415(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_416(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_417(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_418(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_419(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_420(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_421(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_422(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_423(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_424(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_425(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_426(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_427(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_428(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_429(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_430(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_431(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_432(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_433(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_434(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_435(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_436(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_437(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_438(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_439(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_440(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_441(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_442(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_443(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_444(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_445(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_446(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_447(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_448(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_449(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_450(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_451(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_452(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_453(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_454(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_455(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_456(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_457(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_458(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_459(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_460(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_461(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_462(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_463(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_464(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_465(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_466(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_467(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_468(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_469(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_470(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_471(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_472(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_473(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_474(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_475(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_476(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_477(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_478(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_479(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_480(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_481(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_482(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_483(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_484(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_485(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_486(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_487(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_488(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_489(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_490(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_491(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_492(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_493(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_494(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_495(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_496(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_497(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_498(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_499(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_500(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_501(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_502(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_503(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_504(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_505(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_506(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_507(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_508(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_509(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_510(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_511(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_512(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_513(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_514(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_515(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_516(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_517(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_518(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_519(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_520(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_521(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_522(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_523(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_524(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_525(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_526(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_527(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_528(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_529(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_530(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_531(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_532(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_533(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_534(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_535(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_536(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_537(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_538(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_539(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_540(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_541(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_542(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_543(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_544(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_545(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_546(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_547(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_548(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_549(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_550(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_551(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_552(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_553(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_554(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_555(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_556(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_557(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_558(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_559(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_560(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_561(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_562(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_563(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_564(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_565(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_566(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_567(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_568(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_569(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_570(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_571(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_572(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_573(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_574(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_575(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_576(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_577(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_578(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_579(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_580(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_581(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_582(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_583(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_584(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_585(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_586(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_587(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_588(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_589(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_590(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_591(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_592(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_593(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_594(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_595(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_596(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_597(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_598(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_599(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_600(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_601(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_602(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_603(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_604(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_605(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_606(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_607(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_608(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_609(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_610(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_611(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_612(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_613(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_614(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_615(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_616(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_617(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_618(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_619(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_620(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_621(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_622(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_623(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_624(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_625(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_626(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_627(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_628(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_629(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_630(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_631(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_632(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_633(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_634(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_635(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_636(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_637(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_638(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_639(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_640(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_641(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_642(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_643(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_644(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_645(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_646(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_647(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_648(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_649(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_650(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_651(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_652(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_653(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_654(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_655(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_656(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_657(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_658(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_659(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_660(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_661(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_662(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_663(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_664(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_665(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_666(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_667(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_668(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_669(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_670(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_671(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_672(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_673(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_674(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_675(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_676(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_677(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_678(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_679(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_680(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_681(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_682(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_683(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_684(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_685(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_686(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_687(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_688(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_689(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_690(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_691(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_692(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_693(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_694(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_695(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_696(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_697(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_698(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_699(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_700(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_701(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_702(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_703(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_704(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_705(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_706(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_707(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_708(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_709(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_710(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_711(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_712(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_713(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_714(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_715(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_716(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_717(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_718(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_719(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_720(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_721(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_722(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_723(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_724(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_725(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_726(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_727(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_728(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_729(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_730(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_731(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_732(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_733(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_734(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_735(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_736(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_737(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_738(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_739(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_740(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_741(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_742(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_743(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_744(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_745(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_746(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_747(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_748(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_749(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_750(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_751(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_752(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_753(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_754(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_755(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_756(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_757(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_758(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_759(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_760(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_761(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_762(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_763(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_764(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_765(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_766(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_767(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_768(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_769(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_770(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_771(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_772(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_773(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_774(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_775(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_776(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_777(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_778(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_779(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_780(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_781(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_782(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_783(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_784(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_785(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_786(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_787(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_788(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_789(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_790(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_791(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_792(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_793(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_794(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_795(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_796(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_797(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_798(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_799(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_800(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_801(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_802(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_803(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_804(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_805(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_806(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_807(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_808(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_809(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_810(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_811(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_812(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_813(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_814(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_815(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_816(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_817(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_818(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_819(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_820(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_821(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_822(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_823(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_824(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_825(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_826(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_827(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_828(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_829(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_830(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_831(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_832(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_833(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_834(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_835(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_836(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_837(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_838(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_839(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_840(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_841(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_842(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_843(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_844(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_845(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_846(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_847(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_848(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_849(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_850(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_851(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_852(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_853(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_854(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_855(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_856(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_857(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_858(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_859(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_860(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_861(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_862(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_863(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_864(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_865(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_866(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_867(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_868(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_869(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_870(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_871(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_872(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_873(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_874(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_875(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_876(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_877(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_878(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_879(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_880(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_881(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_882(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_883(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_884(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_885(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_886(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_887(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_888(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_889(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_890(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_891(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_892(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_893(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_894(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_895(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_896(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_897(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_898(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_899(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_900(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_901(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_902(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_903(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_904(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_905(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_906(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_907(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_908(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_909(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_910(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_911(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_912(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_913(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_914(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_915(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_916(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_917(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_918(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_919(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_920(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_921(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_922(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_923(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_924(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_925(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_926(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_927(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_928(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_929(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_930(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_931(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_932(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_933(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_934(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_935(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_936(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_937(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_938(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_939(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_940(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_941(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_942(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_943(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_944(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_945(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_946(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_947(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_948(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_949(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_950(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_951(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_952(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_953(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_954(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_955(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_956(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_957(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_958(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_959(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_960(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_961(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_962(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_963(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_964(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_965(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_966(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_967(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_968(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_969(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_970(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_971(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_972(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_973(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_974(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_975(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_976(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_977(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_978(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_979(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_980(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_981(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_982(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_983(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_984(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_985(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_986(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_987(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_988(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_989(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_990(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_991(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_992(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_993(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_994(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_995(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_996(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_997(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_998(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_999(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1000(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1001(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1002(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1003(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1004(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1005(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1006(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1007(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1008(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1009(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1010(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1011(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1012(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1013(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1014(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1015(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1016(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1017(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1018(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1019(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1020(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1021(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1022(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1023(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1024(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1025(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1026(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1027(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1028(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1029(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1030(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1031(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1032(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1033(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1034(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1035(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1036(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1037(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1038(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1039(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1040(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1041(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1042(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1043(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1044(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1045(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1046(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1047(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1048(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1049(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1050(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1051(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1052(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1053(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1054(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1055(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1056(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1057(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1058(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1059(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1060(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1061(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1062(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1063(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1064(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1065(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1066(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1067(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1068(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1069(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1070(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1071(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1072(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1073(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1074(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1075(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1076(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1077(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1078(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1079(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1080(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1081(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1082(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1083(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1084(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1085(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1086(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1087(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1088(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1089(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1090(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1091(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1092(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1093(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1094(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1095(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1096(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1097(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1098(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1099(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1100(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1101(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1102(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1103(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1104(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1105(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1106(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1107(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1108(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1109(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1110(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1111(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1112(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1113(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1114(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1115(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1116(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1117(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1118(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1119(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1120(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1121(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1122(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1123(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1124(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1125(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1126(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1127(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1128(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1129(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1130(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1131(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1132(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1133(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1134(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1135(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1136(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1137(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1138(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1139(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1140(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1141(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1142(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1143(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1144(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1145(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1146(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1147(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1148(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1149(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1150(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1151(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1152(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1153(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1154(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1155(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1156(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1157(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1158(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1159(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1160(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1161(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1162(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1163(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1164(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1165(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1166(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1167(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1168(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1169(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1170(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1171(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1172(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1173(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1174(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1175(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1176(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1177(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1178(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1179(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1180(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1181(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1182(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1183(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1184(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1185(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1186(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1187(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1188(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1189(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1190(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1191(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1192(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1193(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1194(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1195(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1196(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1197(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1198(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_10_1199(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
<!-- END GENERATED API TABLE -->

## Example

```javascript
// mod_10_0 follows the uniform contract: 6*x, then +10 if even (=> 6*x + 10 for integer x)
mod_10_0(5); // === 40   (6*5 + 10)
mod_10_0(1); // === 16   (6*1 + 10)
```

## Source

Source: tests/integration/file_10.js:L3-L10

---

Back to the [API Reference catalog](../../index.md).
