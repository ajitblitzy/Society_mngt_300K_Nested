# mod_26

[← Back to API Reference catalog](../index.md)

## Overview

- **Module identity:** `mod_26`
- **Layer:** `utils`
- **Source file:** `src/utils/file_26.js`
- **Functions:** 1,200 (`mod_26_0` … `mod_26_1199`)

`mod_26` is one of three module identities in the `utils` layer — alongside `mod_4` and `mod_15` — and, like every identity in the codebase, it contains only the uniform arithmetic functions, with no business logic. It belongs to the `utils` layer by directory taxonomy only; it provides **no general-purpose utility helpers**, only the uniform synthetic functions.

> **Synthetic code.** Despite the `utils` layer name, `mod_26` provides no utility helpers; every one of its functions is the same synthetic arithmetic routine.

## Uniform Contract

Every function in this identity has the signature `mod_26_M(x) → number`. It accumulates `x*1 + x*2 + x*3` (= `6*x`) and adds `10` when the running total is even; for integer `x` the result is therefore `6*x + 10`.

All 28 module identities share this **uniform contract** — see the canonical definition in [Code Conventions & Uniform Contract](../../architecture/code-conventions.md). JSDoc is applied to every function per the project rule; see [JSDoc Conventions](../../guides/jsdoc-conventions.md).

## API Reference

The complete function table below is generated from the JSDoc in `src/utils/file_26.js` by `npm run docs:api` (`jsdoc-to-markdown`). It is not hand-maintained.

<!-- BEGIN GENERATED API TABLE -->
**Total functions:** 1200

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `mod_26_0(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_2(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_3(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_4(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_5(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_6(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_7(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_8(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_9(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_10(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_11(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_12(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_13(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_14(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_15(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_16(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_17(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_18(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_19(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_20(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_21(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_22(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_23(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_24(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_25(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_26(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_27(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_28(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_29(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_30(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_31(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_32(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_33(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_34(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_35(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_36(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_37(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_38(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_39(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_40(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_41(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_42(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_43(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_44(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_45(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_46(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_47(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_48(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_49(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_50(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_51(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_52(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_53(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_54(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_55(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_56(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_57(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_58(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_59(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_60(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_61(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_62(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_63(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_64(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_65(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_66(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_67(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_68(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_69(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_70(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_71(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_72(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_73(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_74(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_75(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_76(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_77(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_78(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_79(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_80(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_81(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_82(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_83(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_84(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_85(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_86(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_87(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_88(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_89(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_90(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_91(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_92(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_93(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_94(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_95(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_96(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_97(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_98(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_99(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_100(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_101(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_102(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_103(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_104(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_105(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_106(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_107(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_108(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_109(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_110(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_111(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_112(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_113(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_114(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_115(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_116(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_117(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_118(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_119(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_120(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_121(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_122(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_123(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_124(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_125(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_126(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_127(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_128(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_129(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_130(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_131(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_132(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_133(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_134(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_135(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_136(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_137(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_138(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_139(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_140(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_141(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_142(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_143(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_144(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_145(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_146(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_147(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_148(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_149(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_150(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_151(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_152(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_153(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_154(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_155(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_156(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_157(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_158(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_159(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_160(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_161(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_162(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_163(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_164(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_165(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_166(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_167(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_168(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_169(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_170(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_171(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_172(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_173(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_174(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_175(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_176(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_177(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_178(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_179(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_180(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_181(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_182(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_183(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_184(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_185(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_186(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_187(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_188(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_189(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_190(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_191(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_192(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_193(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_194(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_195(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_196(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_197(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_198(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_199(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_200(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_201(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_202(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_203(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_204(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_205(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_206(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_207(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_208(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_209(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_210(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_211(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_212(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_213(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_214(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_215(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_216(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_217(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_218(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_219(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_220(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_221(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_222(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_223(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_224(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_225(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_226(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_227(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_228(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_229(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_230(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_231(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_232(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_233(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_234(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_235(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_236(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_237(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_238(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_239(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_240(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_241(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_242(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_243(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_244(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_245(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_246(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_247(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_248(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_249(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_250(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_251(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_252(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_253(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_254(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_255(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_256(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_257(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_258(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_259(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_260(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_261(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_262(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_263(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_264(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_265(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_266(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_267(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_268(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_269(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_270(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_271(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_272(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_273(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_274(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_275(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_276(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_277(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_278(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_279(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_280(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_281(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_282(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_283(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_284(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_285(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_286(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_287(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_288(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_289(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_290(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_291(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_292(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_293(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_294(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_295(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_296(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_297(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_298(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_299(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_300(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_301(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_302(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_303(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_304(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_305(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_306(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_307(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_308(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_309(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_310(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_311(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_312(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_313(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_314(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_315(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_316(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_317(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_318(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_319(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_320(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_321(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_322(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_323(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_324(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_325(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_326(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_327(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_328(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_329(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_330(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_331(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_332(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_333(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_334(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_335(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_336(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_337(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_338(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_339(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_340(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_341(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_342(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_343(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_344(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_345(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_346(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_347(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_348(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_349(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_350(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_351(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_352(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_353(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_354(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_355(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_356(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_357(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_358(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_359(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_360(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_361(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_362(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_363(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_364(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_365(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_366(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_367(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_368(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_369(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_370(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_371(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_372(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_373(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_374(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_375(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_376(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_377(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_378(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_379(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_380(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_381(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_382(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_383(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_384(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_385(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_386(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_387(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_388(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_389(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_390(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_391(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_392(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_393(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_394(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_395(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_396(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_397(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_398(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_399(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_400(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_401(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_402(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_403(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_404(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_405(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_406(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_407(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_408(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_409(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_410(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_411(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_412(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_413(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_414(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_415(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_416(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_417(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_418(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_419(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_420(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_421(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_422(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_423(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_424(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_425(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_426(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_427(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_428(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_429(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_430(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_431(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_432(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_433(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_434(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_435(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_436(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_437(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_438(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_439(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_440(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_441(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_442(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_443(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_444(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_445(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_446(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_447(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_448(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_449(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_450(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_451(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_452(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_453(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_454(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_455(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_456(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_457(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_458(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_459(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_460(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_461(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_462(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_463(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_464(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_465(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_466(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_467(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_468(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_469(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_470(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_471(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_472(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_473(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_474(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_475(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_476(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_477(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_478(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_479(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_480(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_481(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_482(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_483(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_484(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_485(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_486(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_487(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_488(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_489(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_490(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_491(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_492(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_493(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_494(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_495(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_496(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_497(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_498(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_499(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_500(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_501(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_502(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_503(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_504(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_505(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_506(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_507(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_508(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_509(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_510(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_511(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_512(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_513(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_514(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_515(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_516(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_517(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_518(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_519(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_520(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_521(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_522(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_523(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_524(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_525(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_526(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_527(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_528(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_529(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_530(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_531(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_532(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_533(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_534(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_535(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_536(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_537(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_538(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_539(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_540(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_541(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_542(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_543(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_544(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_545(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_546(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_547(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_548(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_549(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_550(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_551(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_552(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_553(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_554(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_555(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_556(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_557(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_558(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_559(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_560(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_561(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_562(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_563(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_564(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_565(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_566(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_567(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_568(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_569(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_570(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_571(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_572(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_573(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_574(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_575(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_576(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_577(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_578(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_579(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_580(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_581(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_582(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_583(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_584(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_585(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_586(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_587(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_588(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_589(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_590(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_591(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_592(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_593(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_594(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_595(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_596(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_597(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_598(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_599(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_600(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_601(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_602(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_603(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_604(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_605(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_606(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_607(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_608(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_609(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_610(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_611(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_612(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_613(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_614(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_615(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_616(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_617(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_618(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_619(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_620(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_621(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_622(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_623(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_624(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_625(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_626(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_627(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_628(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_629(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_630(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_631(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_632(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_633(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_634(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_635(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_636(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_637(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_638(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_639(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_640(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_641(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_642(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_643(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_644(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_645(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_646(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_647(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_648(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_649(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_650(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_651(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_652(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_653(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_654(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_655(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_656(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_657(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_658(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_659(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_660(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_661(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_662(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_663(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_664(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_665(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_666(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_667(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_668(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_669(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_670(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_671(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_672(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_673(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_674(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_675(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_676(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_677(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_678(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_679(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_680(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_681(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_682(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_683(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_684(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_685(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_686(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_687(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_688(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_689(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_690(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_691(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_692(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_693(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_694(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_695(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_696(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_697(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_698(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_699(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_700(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_701(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_702(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_703(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_704(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_705(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_706(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_707(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_708(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_709(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_710(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_711(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_712(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_713(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_714(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_715(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_716(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_717(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_718(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_719(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_720(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_721(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_722(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_723(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_724(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_725(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_726(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_727(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_728(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_729(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_730(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_731(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_732(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_733(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_734(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_735(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_736(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_737(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_738(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_739(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_740(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_741(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_742(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_743(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_744(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_745(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_746(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_747(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_748(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_749(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_750(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_751(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_752(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_753(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_754(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_755(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_756(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_757(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_758(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_759(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_760(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_761(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_762(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_763(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_764(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_765(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_766(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_767(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_768(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_769(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_770(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_771(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_772(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_773(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_774(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_775(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_776(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_777(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_778(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_779(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_780(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_781(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_782(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_783(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_784(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_785(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_786(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_787(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_788(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_789(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_790(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_791(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_792(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_793(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_794(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_795(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_796(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_797(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_798(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_799(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_800(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_801(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_802(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_803(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_804(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_805(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_806(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_807(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_808(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_809(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_810(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_811(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_812(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_813(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_814(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_815(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_816(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_817(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_818(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_819(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_820(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_821(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_822(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_823(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_824(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_825(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_826(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_827(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_828(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_829(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_830(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_831(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_832(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_833(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_834(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_835(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_836(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_837(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_838(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_839(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_840(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_841(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_842(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_843(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_844(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_845(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_846(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_847(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_848(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_849(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_850(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_851(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_852(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_853(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_854(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_855(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_856(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_857(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_858(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_859(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_860(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_861(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_862(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_863(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_864(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_865(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_866(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_867(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_868(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_869(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_870(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_871(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_872(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_873(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_874(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_875(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_876(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_877(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_878(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_879(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_880(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_881(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_882(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_883(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_884(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_885(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_886(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_887(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_888(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_889(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_890(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_891(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_892(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_893(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_894(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_895(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_896(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_897(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_898(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_899(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_900(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_901(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_902(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_903(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_904(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_905(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_906(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_907(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_908(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_909(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_910(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_911(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_912(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_913(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_914(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_915(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_916(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_917(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_918(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_919(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_920(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_921(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_922(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_923(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_924(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_925(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_926(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_927(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_928(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_929(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_930(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_931(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_932(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_933(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_934(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_935(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_936(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_937(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_938(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_939(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_940(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_941(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_942(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_943(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_944(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_945(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_946(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_947(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_948(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_949(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_950(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_951(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_952(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_953(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_954(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_955(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_956(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_957(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_958(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_959(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_960(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_961(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_962(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_963(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_964(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_965(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_966(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_967(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_968(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_969(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_970(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_971(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_972(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_973(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_974(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_975(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_976(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_977(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_978(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_979(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_980(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_981(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_982(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_983(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_984(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_985(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_986(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_987(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_988(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_989(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_990(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_991(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_992(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_993(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_994(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_995(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_996(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_997(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_998(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_999(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1000(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1001(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1002(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1003(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1004(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1005(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1006(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1007(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1008(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1009(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1010(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1011(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1012(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1013(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1014(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1015(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1016(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1017(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1018(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1019(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1020(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1021(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1022(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1023(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1024(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1025(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1026(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1027(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1028(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1029(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1030(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1031(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1032(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1033(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1034(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1035(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1036(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1037(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1038(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1039(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1040(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1041(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1042(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1043(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1044(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1045(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1046(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1047(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1048(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1049(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1050(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1051(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1052(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1053(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1054(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1055(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1056(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1057(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1058(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1059(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1060(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1061(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1062(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1063(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1064(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1065(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1066(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1067(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1068(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1069(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1070(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1071(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1072(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1073(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1074(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1075(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1076(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1077(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1078(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1079(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1080(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1081(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1082(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1083(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1084(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1085(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1086(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1087(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1088(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1089(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1090(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1091(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1092(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1093(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1094(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1095(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1096(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1097(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1098(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1099(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1100(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1101(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1102(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1103(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1104(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1105(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1106(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1107(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1108(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1109(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1110(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1111(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1112(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1113(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1114(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1115(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1116(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1117(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1118(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1119(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1120(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1121(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1122(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1123(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1124(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1125(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1126(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1127(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1128(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1129(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1130(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1131(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1132(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1133(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1134(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1135(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1136(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1137(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1138(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1139(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1140(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1141(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1142(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1143(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1144(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1145(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1146(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1147(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1148(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1149(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1150(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1151(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1152(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1153(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1154(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1155(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1156(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1157(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1158(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1159(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1160(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1161(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1162(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1163(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1164(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1165(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1166(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1167(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1168(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1169(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1170(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1171(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1172(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1173(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1174(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1175(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1176(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1177(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1178(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1179(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1180(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1181(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1182(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1183(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1184(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1185(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1186(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1187(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1188(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1189(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1190(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1191(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1192(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1193(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1194(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1195(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1196(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1197(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1198(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_26_1199(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
<!-- END GENERATED API TABLE -->

## Example

```javascript
// mod_26_0 computes 6*x, then +10 because the result is even.
mod_26_0(5); // => 40   (6*5 + 10)
mod_26_0(1); // => 16   (6*1 + 10)
```

## Source Citation

Source: src/utils/file_26.js:L3-L10

> Note: `src/utils/filler.js` in this layer is a non-identity padding artifact (0 functions) and has no reference page.

[← API Reference catalog](../index.md)
