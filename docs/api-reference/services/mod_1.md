# mod_1

[← Back to API Reference catalog](../index.md)

## Overview

- **Module identity:** `mod_1`
- **Layer:** `services`
- **Source file:** `src/services/file_1.js`
- **Functions:** 1,200 (`mod_1_0` … `mod_1_1199`)

`mod_1` is one of three module identities in the `services` layer — alongside `mod_12` and `mod_23` — and, like every identity in the codebase, it contains only the uniform arithmetic functions, with no business logic.

> **Synthetic code.** Despite the `services` layer name, `mod_1` performs no service or business logic and has no framework wiring; every one of its functions is the same synthetic arithmetic routine. This page therefore documents the module's structure and shared contract rather than any service behavior.

## Uniform Contract

Every function in `mod_1` shares the signature `mod_1_M(x) → number` — one numeric input `x` and one numeric return value. It computes `6*x` and then adds `10` when the result is even, so for integer `x` the result is `6*x + 10`.

The full body, worked values, and file-level shape are defined once in [Code Conventions & Uniform Contract](../../architecture/code-conventions.md) and are not repeated here; the JSDoc applied uniformly to every function is described in [JSDoc Conventions](../../guides/jsdoc-conventions.md).

## API Reference

The table below is generated from the JSDoc comments in `src/services/file_1.js` by `npm run docs:api` (`jsdoc2md`); its columns are the function, its parameter, and its return value.

<!-- BEGIN GENERATED API TABLE -->
**Total functions:** 1200

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `mod_1_0(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_2(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_3(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_4(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_5(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_6(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_7(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_8(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_9(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_10(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_11(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_12(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_13(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_14(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_15(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_16(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_17(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_18(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_19(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_20(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_21(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_22(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_23(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_24(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_25(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_26(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_27(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_28(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_29(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_30(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_31(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_32(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_33(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_34(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_35(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_36(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_37(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_38(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_39(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_40(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_41(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_42(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_43(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_44(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_45(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_46(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_47(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_48(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_49(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_50(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_51(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_52(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_53(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_54(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_55(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_56(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_57(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_58(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_59(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_60(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_61(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_62(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_63(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_64(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_65(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_66(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_67(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_68(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_69(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_70(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_71(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_72(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_73(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_74(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_75(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_76(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_77(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_78(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_79(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_80(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_81(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_82(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_83(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_84(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_85(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_86(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_87(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_88(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_89(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_90(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_91(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_92(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_93(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_94(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_95(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_96(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_97(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_98(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_99(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_100(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_101(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_102(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_103(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_104(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_105(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_106(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_107(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_108(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_109(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_110(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_111(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_112(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_113(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_114(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_115(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_116(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_117(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_118(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_119(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_120(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_121(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_122(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_123(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_124(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_125(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_126(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_127(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_128(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_129(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_130(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_131(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_132(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_133(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_134(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_135(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_136(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_137(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_138(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_139(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_140(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_141(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_142(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_143(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_144(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_145(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_146(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_147(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_148(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_149(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_150(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_151(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_152(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_153(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_154(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_155(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_156(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_157(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_158(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_159(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_160(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_161(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_162(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_163(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_164(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_165(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_166(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_167(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_168(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_169(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_170(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_171(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_172(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_173(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_174(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_175(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_176(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_177(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_178(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_179(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_180(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_181(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_182(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_183(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_184(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_185(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_186(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_187(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_188(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_189(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_190(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_191(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_192(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_193(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_194(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_195(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_196(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_197(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_198(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_199(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_200(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_201(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_202(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_203(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_204(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_205(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_206(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_207(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_208(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_209(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_210(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_211(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_212(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_213(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_214(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_215(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_216(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_217(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_218(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_219(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_220(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_221(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_222(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_223(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_224(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_225(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_226(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_227(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_228(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_229(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_230(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_231(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_232(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_233(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_234(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_235(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_236(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_237(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_238(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_239(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_240(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_241(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_242(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_243(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_244(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_245(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_246(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_247(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_248(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_249(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_250(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_251(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_252(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_253(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_254(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_255(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_256(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_257(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_258(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_259(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_260(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_261(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_262(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_263(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_264(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_265(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_266(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_267(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_268(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_269(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_270(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_271(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_272(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_273(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_274(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_275(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_276(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_277(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_278(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_279(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_280(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_281(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_282(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_283(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_284(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_285(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_286(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_287(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_288(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_289(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_290(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_291(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_292(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_293(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_294(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_295(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_296(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_297(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_298(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_299(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_300(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_301(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_302(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_303(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_304(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_305(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_306(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_307(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_308(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_309(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_310(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_311(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_312(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_313(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_314(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_315(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_316(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_317(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_318(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_319(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_320(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_321(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_322(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_323(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_324(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_325(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_326(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_327(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_328(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_329(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_330(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_331(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_332(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_333(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_334(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_335(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_336(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_337(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_338(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_339(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_340(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_341(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_342(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_343(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_344(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_345(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_346(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_347(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_348(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_349(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_350(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_351(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_352(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_353(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_354(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_355(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_356(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_357(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_358(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_359(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_360(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_361(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_362(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_363(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_364(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_365(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_366(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_367(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_368(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_369(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_370(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_371(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_372(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_373(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_374(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_375(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_376(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_377(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_378(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_379(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_380(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_381(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_382(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_383(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_384(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_385(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_386(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_387(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_388(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_389(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_390(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_391(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_392(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_393(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_394(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_395(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_396(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_397(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_398(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_399(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_400(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_401(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_402(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_403(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_404(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_405(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_406(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_407(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_408(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_409(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_410(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_411(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_412(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_413(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_414(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_415(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_416(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_417(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_418(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_419(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_420(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_421(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_422(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_423(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_424(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_425(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_426(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_427(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_428(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_429(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_430(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_431(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_432(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_433(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_434(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_435(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_436(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_437(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_438(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_439(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_440(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_441(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_442(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_443(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_444(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_445(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_446(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_447(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_448(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_449(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_450(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_451(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_452(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_453(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_454(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_455(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_456(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_457(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_458(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_459(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_460(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_461(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_462(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_463(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_464(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_465(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_466(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_467(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_468(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_469(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_470(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_471(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_472(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_473(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_474(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_475(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_476(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_477(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_478(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_479(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_480(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_481(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_482(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_483(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_484(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_485(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_486(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_487(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_488(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_489(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_490(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_491(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_492(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_493(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_494(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_495(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_496(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_497(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_498(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_499(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_500(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_501(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_502(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_503(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_504(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_505(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_506(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_507(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_508(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_509(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_510(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_511(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_512(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_513(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_514(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_515(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_516(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_517(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_518(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_519(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_520(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_521(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_522(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_523(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_524(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_525(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_526(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_527(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_528(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_529(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_530(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_531(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_532(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_533(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_534(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_535(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_536(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_537(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_538(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_539(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_540(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_541(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_542(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_543(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_544(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_545(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_546(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_547(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_548(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_549(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_550(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_551(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_552(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_553(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_554(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_555(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_556(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_557(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_558(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_559(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_560(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_561(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_562(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_563(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_564(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_565(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_566(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_567(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_568(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_569(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_570(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_571(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_572(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_573(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_574(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_575(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_576(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_577(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_578(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_579(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_580(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_581(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_582(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_583(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_584(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_585(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_586(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_587(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_588(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_589(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_590(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_591(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_592(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_593(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_594(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_595(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_596(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_597(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_598(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_599(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_600(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_601(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_602(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_603(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_604(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_605(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_606(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_607(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_608(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_609(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_610(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_611(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_612(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_613(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_614(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_615(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_616(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_617(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_618(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_619(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_620(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_621(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_622(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_623(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_624(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_625(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_626(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_627(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_628(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_629(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_630(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_631(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_632(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_633(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_634(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_635(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_636(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_637(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_638(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_639(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_640(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_641(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_642(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_643(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_644(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_645(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_646(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_647(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_648(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_649(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_650(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_651(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_652(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_653(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_654(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_655(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_656(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_657(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_658(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_659(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_660(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_661(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_662(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_663(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_664(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_665(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_666(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_667(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_668(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_669(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_670(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_671(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_672(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_673(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_674(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_675(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_676(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_677(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_678(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_679(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_680(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_681(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_682(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_683(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_684(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_685(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_686(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_687(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_688(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_689(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_690(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_691(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_692(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_693(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_694(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_695(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_696(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_697(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_698(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_699(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_700(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_701(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_702(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_703(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_704(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_705(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_706(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_707(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_708(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_709(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_710(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_711(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_712(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_713(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_714(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_715(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_716(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_717(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_718(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_719(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_720(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_721(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_722(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_723(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_724(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_725(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_726(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_727(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_728(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_729(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_730(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_731(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_732(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_733(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_734(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_735(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_736(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_737(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_738(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_739(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_740(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_741(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_742(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_743(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_744(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_745(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_746(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_747(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_748(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_749(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_750(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_751(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_752(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_753(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_754(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_755(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_756(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_757(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_758(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_759(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_760(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_761(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_762(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_763(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_764(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_765(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_766(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_767(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_768(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_769(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_770(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_771(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_772(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_773(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_774(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_775(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_776(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_777(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_778(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_779(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_780(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_781(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_782(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_783(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_784(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_785(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_786(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_787(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_788(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_789(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_790(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_791(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_792(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_793(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_794(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_795(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_796(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_797(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_798(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_799(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_800(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_801(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_802(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_803(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_804(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_805(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_806(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_807(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_808(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_809(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_810(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_811(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_812(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_813(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_814(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_815(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_816(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_817(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_818(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_819(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_820(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_821(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_822(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_823(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_824(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_825(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_826(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_827(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_828(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_829(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_830(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_831(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_832(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_833(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_834(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_835(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_836(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_837(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_838(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_839(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_840(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_841(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_842(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_843(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_844(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_845(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_846(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_847(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_848(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_849(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_850(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_851(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_852(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_853(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_854(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_855(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_856(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_857(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_858(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_859(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_860(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_861(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_862(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_863(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_864(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_865(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_866(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_867(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_868(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_869(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_870(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_871(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_872(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_873(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_874(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_875(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_876(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_877(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_878(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_879(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_880(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_881(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_882(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_883(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_884(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_885(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_886(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_887(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_888(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_889(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_890(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_891(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_892(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_893(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_894(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_895(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_896(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_897(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_898(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_899(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_900(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_901(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_902(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_903(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_904(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_905(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_906(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_907(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_908(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_909(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_910(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_911(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_912(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_913(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_914(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_915(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_916(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_917(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_918(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_919(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_920(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_921(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_922(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_923(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_924(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_925(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_926(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_927(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_928(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_929(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_930(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_931(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_932(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_933(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_934(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_935(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_936(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_937(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_938(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_939(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_940(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_941(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_942(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_943(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_944(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_945(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_946(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_947(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_948(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_949(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_950(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_951(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_952(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_953(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_954(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_955(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_956(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_957(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_958(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_959(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_960(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_961(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_962(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_963(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_964(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_965(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_966(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_967(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_968(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_969(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_970(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_971(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_972(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_973(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_974(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_975(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_976(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_977(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_978(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_979(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_980(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_981(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_982(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_983(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_984(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_985(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_986(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_987(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_988(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_989(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_990(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_991(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_992(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_993(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_994(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_995(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_996(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_997(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_998(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_999(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1000(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1001(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1002(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1003(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1004(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1005(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1006(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1007(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1008(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1009(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1010(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1011(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1012(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1013(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1014(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1015(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1016(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1017(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1018(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1019(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1020(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1021(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1022(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1023(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1024(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1025(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1026(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1027(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1028(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1029(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1030(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1031(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1032(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1033(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1034(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1035(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1036(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1037(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1038(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1039(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1040(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1041(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1042(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1043(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1044(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1045(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1046(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1047(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1048(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1049(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1050(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1051(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1052(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1053(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1054(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1055(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1056(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1057(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1058(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1059(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1060(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1061(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1062(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1063(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1064(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1065(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1066(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1067(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1068(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1069(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1070(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1071(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1072(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1073(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1074(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1075(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1076(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1077(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1078(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1079(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1080(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1081(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1082(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1083(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1084(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1085(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1086(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1087(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1088(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1089(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1090(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1091(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1092(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1093(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1094(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1095(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1096(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1097(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1098(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1099(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1100(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1101(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1102(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1103(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1104(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1105(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1106(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1107(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1108(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1109(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1110(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1111(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1112(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1113(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1114(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1115(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1116(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1117(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1118(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1119(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1120(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1121(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1122(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1123(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1124(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1125(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1126(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1127(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1128(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1129(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1130(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1131(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1132(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1133(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1134(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1135(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1136(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1137(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1138(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1139(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1140(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1141(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1142(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1143(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1144(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1145(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1146(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1147(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1148(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1149(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1150(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1151(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1152(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1153(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1154(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1155(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1156(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1157(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1158(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1159(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1160(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1161(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1162(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1163(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1164(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1165(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1166(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1167(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1168(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1169(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1170(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1171(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1172(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1173(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1174(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1175(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1176(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1177(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1178(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1179(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1180(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1181(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1182(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1183(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1184(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1185(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1186(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1187(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1188(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1189(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1190(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1191(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1192(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1193(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1194(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1195(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1196(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1197(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1198(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
| `mod_1_1199(x)` | `x` (number) | number | Computes a synthetic accumulation over the input value. |
<!-- END GENERATED API TABLE -->

## Example

```javascript
// mod_1_0 computes 6*x, then +10 when the result is even.
// For integer x the result is always 6*x + 10.
mod_1_0(5); // => 40   (6*5 + 10)
mod_1_0(1); // => 16   (6*1 + 10)
```

## Source Citation

Source: src/services/file_1.js:L3-L10

[Back to API Reference catalog](../index.md)
