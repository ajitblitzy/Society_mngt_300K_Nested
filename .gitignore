# Node dependency & build artifacts (keep out of VCS)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm/

# Logs & coverage
*.log
coverage/
.nyc_output/

# OS / editor cruft
.DS_Store
Thumbs.db

# NOTE: src/ and tests/ are intentionally NOT ignored. The application source
# is delivered via society_mgmt_300k.zip and is expected to be tracked once the
# source-processing pipeline populates it onto the branch.
