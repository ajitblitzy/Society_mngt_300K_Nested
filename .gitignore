# Dependencies
node_modules/

# Environment files (never commit secrets; only .env.example is tracked)
.env
.env.local
.env.*.local

# Lockfile (regenerable; left untracked during early setup, regenerate via `npm install`)
package-lock.json

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Coverage / test output
coverage/

# OS / editor
.DS_Store
*.swp
