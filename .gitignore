# Dependencies
node_modules/

# Lockfile: generated by `npm install`. Kept on disk for reproducible installs
# but intentionally not committed (no lockfile was previously tracked).
package-lock.json

# Logs / debug output
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS / editor cruft
.DS_Store
Thumbs.db

# Blitzy ad-hoc test artifacts
blitzy_adhoc_test_*

# NOTE: The project source is delivered inside society_mgmt_300k.zip (the
# committed source of truth) and is extracted to src/ and tests/ via
# `npm run unpack`. Those extracted trees are reproducible build inputs.
# They are intentionally NOT listed here so that any first-class source later
# placed under src/ or tests/ can be tracked normally.
