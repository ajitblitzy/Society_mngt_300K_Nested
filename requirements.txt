# Runtime dependencies for the planned JavaScript/Node.js -> Python migration target.
# Source of versions: Agent Action Plan (AAP) Section 0.5.1.
#
# NOTE (setup baseline): The repository currently contains NO application source yet
# (the JS->Python migration is future work, per the AAP blocking precondition).
# These pins establish the target Python baseline environment so that future
# implementation work can install, build, run, and test immediately.
#
# Exact pins  = AAP-verified core framework versions (June 2026).
# Range specs = AAP-specified minimum versions.

fastapi==0.136.1
pydantic==2.10.4
pydantic-settings>=2.0
sqlalchemy==2.0.36
alembic==1.14.0
uvicorn==0.34.0
httpx>=0.28
aiosqlite>=0.20