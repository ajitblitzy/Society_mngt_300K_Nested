"""society_mgmt.services — services layer package.

Structural label only: this package implements no service / business-logic
behavior, because none existed in the original JavaScript source. It groups
the name-preserving modules ``file_1``, ``file_12`` and ``file_23``, each of
which re-exposes its original ``mod_N_K`` functions as thin bindings that
delegate to :func:`society_mgmt.core.society_compute`.
"""
