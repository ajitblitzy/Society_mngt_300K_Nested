"""config layer package for society_mgmt.

Structural label only — this package holds no configuration or settings
behavior (none existed in the original JavaScript source). It groups the
name-preserving function bindings ported from ``src/config/*.js``; every
binding delegates to :func:`society_mgmt.core.society_compute`.
"""
