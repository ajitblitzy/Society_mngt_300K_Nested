{
  "name": "society-mgmt-300k",
  "version": "1.0.0",
  "description": "test project for backprop integration (Ajit-backprop-test)",
  "license": "MIT",
  "private": true,
  "engines": {
    "node": ">=20.20.2"
  },
  "scripts": {
    "build": "find src tests -name \"*.js\" -print0 | xargs -0 -n1 node --check && echo \"build: all JS files passed node --check\"",
    "lint": "find src tests -name \"*.js\" -print0 | xargs -0 -n1 node --check",
    "test": "node --test tests/unit tests/integration"
  },
  "dependencies": {},
  "devDependencies": {}
}
